<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prajjawalan.prajjwalan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:label="Prajjwalan"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CommonActivity"
            android:label="Events"
            android:parentActivityName="com.prajjawalan.prajjwalan.Home"/>

        <activity android:name=".RegisterActivity"/>
        <activity
            android:name=".GoogleSignIn"
            android:label="Sign In" />
        <activity
            android:name=".Home"
            android:label="Home"
            android:theme="@style/AppTheme" />

        <activity android:name=".EventDetail"
            android:parentActivityName="com.prajjawalan.prajjwalan.CommonActivity"/>

        <activity android:name=".Developers"
            android:label="Developers"/>


    </application>

</manifest>